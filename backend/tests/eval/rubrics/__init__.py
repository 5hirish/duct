"""Per-agent rubrics for the evaluation harness.

Each module here defines the rubric (and any artifact-rendering helper) for one
agent deliverable. The harness core stays agent-agnostic; this is where
agent-specific "what good looks like" lives.
"""
