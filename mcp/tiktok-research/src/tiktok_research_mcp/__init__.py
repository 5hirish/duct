"""TikTok Research MCP — Apify-backed keyword / hashtag / URL discovery.

A self-contained, global MCP server (stdio) for TikTok research and content
discovery. Extracted from the Duct backend's Apify integration
(backend/service/apify) so it can be used from Claude Code and the Claude
Desktop app on any project, with no dependency on the Duct backend or its DB.
"""

__version__ = "0.1.0"
