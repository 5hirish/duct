// ─── Duct Analytics Config ───────────────────────────────────────────────────
// Replace placeholder values before going live.
// GTM is the only ID needed here — all other tags (GA4, Google Ads, X pixel)
// are configured inside the GTM dashboard, not in this file.
// ─────────────────────────────────────────────────────────────────────────────

var DUCT_CONFIG = {
  gtm: 'GTM-PKL589SW'  // google.com/tagmanager → Admin → Container ID
};
