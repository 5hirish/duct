<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Extra Info.plist keys merged into the generated macOS bundle by Tauri.
  Applies to every macOS build (DMG and App Store alike) — both keys below are
  wanted in either case.

  CFBundleVersion is NOT set here: Tauri derives it from `version` in
  tauri.conf.json, and CI overwrites it with the workflow run number so every
  TestFlight upload carries a unique build number.
-->
<plist version="1.0">
<dict>
    <!-- Required by App Store Connect for Mac App Store submissions. -->
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.business</string>

    <!-- Standard TLS only (HTTPS to app.getduct.ai and the Railway API), which
         is exempt from export compliance. Declaring it here means TestFlight
         uploads skip the per-build encryption prompt. -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
</dict>
</plist>
