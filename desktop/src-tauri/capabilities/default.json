{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the hosted Duct web app (and the local dev server) call the keychain commands.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://app.getduct.ai", "http://localhost:3003"]
  },
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
