{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the hosted Duct web app call the keychain commands.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://app.getduct.ai"]
  },
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
